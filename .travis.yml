language: python

python:
  - "2.6"
  - "2.7"
  - "3.5"
 
# command to install dependencies
install:
  - "pip install -I -r requirements.txt"

# command to run tests
script: 
  - flake8
  - pytest
  
after_success:
  coveralls

sudo: false
cache: pip
